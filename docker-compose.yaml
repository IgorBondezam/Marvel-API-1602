version: "3.9"
services:
  db:
    image: postgres
    container_name: banco_postegres
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: kanban
      POSTGRES_DB: kanban
      POSTGRES_PASSWORD: kanban